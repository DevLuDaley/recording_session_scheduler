<p class="navbar-text pull-right">
  <% if engineer_signed_in? %>
  Logged in as <strong><%= current_engineer.name %></strong>. <br>

  <%= link_to 'Edit profile', edit_engineer_registration_path, :class => 'navbar-link' %> | <br>
  <%= link_to 'View Recording Sessions', recording_sessions_path, :class => 'navbar-link' %> | <br>
  <%= link_to "Logout", destroy_engineer_session_path, method: :delete, :class => 'navbar-link'  %>
  <% else %>
  <%= link_to "Sign up", new_engineer_registration_path, :class => 'navbar-link'  %> |
  <%= link_to "Login", new_engineer_session_path, :class => 'navbar-link'  %>
  <% end %>
</p>

<body>


  <% if notice %>
  <p class="alert alert-success"><%= notice %></p>
  <% end %>
  <% if alert %>
  <p class="alert alert-danger"><%= alert %></p>
  <% end %>