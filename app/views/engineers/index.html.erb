<!-- USED in views/RecordingSessions/index -->

<!-- EDIT FORM -->
<!-- DISPLAY recording_session.ALL -->
<section class="main">

    <%= link_to 'logout', destroy_user_session_path, method: :delete %>


    <% if @engineers.blank? %>
    <h2>There are currently no engineers.</h2>
    <% else %>
    <h2>Current Engineers</h1>
        <br>

        <table>
            <thead>
                <tr>
                    <th>ID</th>
                    <th>Engineer</th>
                    <th>Cancel</th>
                    <th colspan="3"></th>
                </tr>
            </thead>

            <% @engineers.each do |c| %>
            <div class="well">
                <tbody>
                        <tr>
                            <td><%= link_to c.id, recording_session_path(c)%></td>

                            <td><%= link_to c.name.capitalize, engineer_path(c) %></a></td>

                            <td>
                                <%= link_to "Delete", c, method: :delete, data: { confirm: 'Are you sure you want to delete this studio?' } %>
                            </td>
                        </tr>
                    <% end %>
                </tbody>
        </table>

        <p> <% end %> </p>

    <!-- URL LINKS -->

    <br>
     <p>
        <%= link_to 'Create New Session', new_recording_session_path %> |
        <%= link_to 'Create New Studio', new_studio_path %> |
        <%= link_to 'Create New Artist', new_artist_path %> |
        <%= link_to 'Create New Engineer', new_engineer_path %>
    </p>