<body>

    <!-- RECORDING SESSION INFO -->
    <div>
        <h2> Comment Page </h2>
        <h3> Content: <%= @comment.content.capitalize %> <button><%= link_to 'Edit Comment', edit_comment_path %></button> </h3>
        <br>

        <% if @comments.blank? %>
        <h2>There are currently no sessions scheduled.</h2>
        <% else %>
        <h2>Scheduled Sessions</h1>
            <br>

            <table>
                <thead>
                    <tr>
                        <th>ID</th>
                        <th>Comment</th>
                        <th>Created</th>
                        <th colspan="3"></th>
                    </tr>
                </thead>

                <tbody>
                    <tr>
                        <td><%= link_to @comment.id, comment_path%></td>
                        <td><%= link_to @comment.content, comment_path%></td>
                        <td><%= link_to @comment.created_at.strftime("%A %B %eth %Y at%l:%M %P"), comment_path%></td>
                        <td><a href="/comments/@comment_id"><%= link_to "Delete", method: :delete, data: { confirm: 'Are you sure you want to delete this comment?' } %></a>
                        </td>

                    </tr>
                </tbody>
            </table>

            <p> <% end %> </p>

    <!-- URL LINKS -->

<br>
<p>
    <%= link_to 'New Session', new_recording_session_path(@artist) %> |
    <%= link_to 'Create New Studio', new_studio_path %> |
    <%= link_to 'Create New Artist', new_artist_path %> |
    <%= link_to 'Create New Engineer', new_engineer_path %>
</p>

<td><a
        href="/comments/@comment_id"><%= link_to "Delete comment", @artist, method: :delete, data: { confirm: 'Are you sure you want to delete this this studio?' } %></a> </p>
</td>

            <!-- END OF EDIT FORM -->